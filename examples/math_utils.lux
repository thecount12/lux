// math_utils.lux - Mathematical utility functions

fun add(a, b) {
    return a + b;
}

fun multiply(a, b) {
    return a * b;
}

fun square(x) {
    return x * x;
}

fun factorial(n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}

print("Math utilities loaded");
