# Paste from `aws sts get-session-token` / SSO / console "Option 2"
# or copy a profile block from ~/.aws/credentials

[960491734164_AccountAdmin]
aws_access_key_id=ASIAEXAMPLE
aws_secret_access_key=example-secret
aws_session_token=example-session-token
region=us-east-1
# bucket=my-bucket
