// Test luxlint: unused vars, unreachable code, duplicate functions, shadowed globals

var unusedGlobal = 1;
var shadowed = 99;

fun duplicate() {
    return 1;
}

fun duplicate() {
    return 2;
}

fun foo() {
    var shadowed = 10;
    return shadowed;
}

fun bar() {
    var unusedLocal = 42;
    return 1;
}

fun baz() {
    return 3;
    print "unreachable";
}

fun qux() {
    if (true) {
        return 4;
        print "also unreachable";
    }
}
