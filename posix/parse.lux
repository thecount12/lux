// poc cloud formation parsing

var fileName = "dat.tx";

fun File(f) {
	var file = fileExists(f);
	print "debug: " + file;
	print "debug type: " + typeof(file);
	
	if (file == true) {
		print "debug inside if statement";
		var dat = readFile(f);
		print dat;
	} else {
		print "File Does not exist: " + f;
	}
	
}

File(fileName);

